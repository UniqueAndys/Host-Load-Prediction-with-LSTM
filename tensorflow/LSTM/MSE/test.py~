#def truncated_index(long_period, num, reverse=True):
#    index = []
#    single_period_len = int(long_period/num)
#    for i in range(num):
#        if reverse:
#            j = num-1 - i
#        else:
#            j = i
#        index.append(range(j*single_period_len, (j+1)*single_period_len))
#    return single_period_len, index
#    
#single_len, index = truncated_index(24,6, False)
#
#import numpy as np
#a = np.arange(20,44)
#for i in range(6):
#    print(a[index[i]])

import time
time1 = time.time()
time.sleep(3)
time2 = time.time()
print time2 - time1

from time import clock
start = clock()
time.sleep(3)
finish = clock()
print finish - start